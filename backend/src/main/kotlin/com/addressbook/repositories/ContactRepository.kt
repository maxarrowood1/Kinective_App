package com.addressbook.repositories

class ContactRepository {
    // TODO: findAll(): List<ContactEntity>                   — query all rows from Contacts table
    // TODO: findById(id: Int): ContactEntity?               — query single row by primary key
    // TODO: create(request: CreateContactRequest): ContactEntity — insert row, return created entity
    // TODO: update(id: Int, request: UpdateContactRequest): ContactEntity? — update row, return updated entity
    // TODO: delete(id: Int): Boolean                        — delete row, return true if deleted
}
