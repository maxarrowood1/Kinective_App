package com.addressbook.services

class ContactService {
    // TODO: getAllContacts()              — delegate to ContactRepository.findAll()
    // TODO: getContactById(id: Int)      — delegate to ContactRepository.findById(), throw 404 if null
    // TODO: createContact(request)       — validate uniqueness, delegate to ContactRepository.create()
    // TODO: updateContact(id, request)   — delegate to ContactRepository.update(), throw 404 if not found
    // TODO: deleteContact(id: Int)       — delegate to ContactRepository.delete(), throw 404 if not found
}
